IMPL-THEME_INJECTION:
  code_locations:
    files:
      - description: Theme injection in layout
        path: src/app/layout.tsx
    functions: []
  cross_references:
    - ARCH-THEME_INJECTION
    - REQ-CONFIG_DRIVEN_UI
  decision: The root layout component renders a `<style>` tag in `<head>` containing CSS custom properties for both light and dark modes, generated from `config/theme.yaml` via `generateThemeCss()`.
  implementation_approach:
    details:
      - Generate CSS variable declarations
      - Inject via style attribute on body
      - Load theme config in layout
    summary: Generate CSS from theme.yaml and inject via head style tag in RootLayout
  metadata:
    created:
      author: AI Agent
      date: {}
    last_updated:
      author: AI Agent
      date: '2026-08-27'
      reason: 'E1 tranche 3: migrate legacy CONTRACT blocks to fenced Layer-B format'
    last_validated:
      date: {}
      result: pass
      validator: AI Agent
  name: Theme Injection Implementation
  rationale:
    benefits:
      - Config-driven CSS variables
      - Dynamic theming
    problems_solved:
      - No config-driven colors
      - Static CSS variables
    why: Injects CSS variables from theme config into root layout
  related_decisions:
    depends_on: []
    see_also:
      - IMPL-CONFIG_LOADER
      - IMPL-DARK_MODE
    supersedes: []
  status: Active
  traceability:
    architecture:
      - ARCH-THEME_INJECTION
    code_annotations:
      - IMPL-THEME_INJECTION
      - REQ-CONFIG_DRIVEN_UI
    requirements:
      - REQ-CONFIG_DRIVEN_UI
    tests:
      - src/app/layout.test.tsx
      - src/lib/config.test.ts
      - src/test/dark-mode.test.tsx
