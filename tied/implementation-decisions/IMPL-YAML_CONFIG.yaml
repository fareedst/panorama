IMPL-YAML_CONFIG:
  code_locations:
    files:
      - description: Site configuration
        path: config/site.yaml
      - description: Theme configuration
        path: config/theme.yaml
    functions: []
  cross_references:
    - ARCH-CONFIG_DRIVEN_UI
    - REQ-CONFIG_DRIVEN_UI
  decision: Two YAML configuration files define all customizable aspects of the template UI.
  essence_pseudocode_path: implementation-decisions/IMPL-YAML_CONFIG-pseudocode.md
  implementation_approach:
    details:
      - Both support partial configs with defaults
      - config/site.yaml for content (metadata, branding, links)
      - config/theme.yaml for styling (colors, spacing, overrides)
    summary: 'Define two YAML files: site.yaml and theme.yaml'
  metadata:
    created:
      author: AI Agent
      date: {}
    last_updated:
      author: AI Agent
      date: '2026-05-30T00:00:00.000Z'
      reason: Retrofit essence_pseudocode from config loader tests
    last_validated:
      date: {}
      result: pass
      validator: AI Agent
  name: YAML Configuration Structure
  rationale:
    benefits:
      - Easy editing
      - Structured configuration
    problems_solved:
      - Hard-coded configuration
      - Lack of structure
    why: Defines YAML configuration file structure for site and theme
  related_decisions:
    depends_on: []
    see_also:
      - IMPL-CONFIG_LOADER
    supersedes: []
  status: Active
  traceability:
    architecture:
      - ARCH-CONFIG_DRIVEN_UI
    code_annotations:
      - REQ-CONFIG_DRIVEN_UI
    requirements:
      - REQ-CONFIG_DRIVEN_UI
    tests:
      - src/lib/config.test.ts
