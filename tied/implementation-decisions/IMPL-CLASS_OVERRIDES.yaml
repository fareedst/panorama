IMPL-CLASS_OVERRIDES:
  code_locations:
    files:
      - path: config/theme.yaml
        description: overrides section schema
      - path: src/lib/config.types.ts
        description: ClassOverrides interface
      - path: src/lib/config.ts
        description: getOverride resolver
  cross_references:
    - ARCH-CLASS_OVERRIDES
    - REQ-CONFIG_DRIVEN_UI
  decision: Per-element Tailwind class overrides from `config/theme.yaml` are merged with component default classes using `tailwind-merge` (`twMerge`).
  implementation_approach:
    details:
      - Apply to components
      - Define class overrides in theme config
      - Install tailwind-merge
      - Merge default classes with overrides using twMerge
    summary: Use tailwind-merge to merge default and override classes
  metadata:
    created:
      author: AI Agent
      date: 2025-11-08T00:00:00.000Z
    last_updated:
      author: AI Agent
      date: "2026-05-29T00:00:00.000Z"
      reason: Retrofit essence_pseudocode from getOverride tests and config types
    last_validated:
      date: 2026-02-06T00:00:00.000Z
      result: pass
      validator: AI Agent
  name: Class Overrides Implementation
  rationale:
    benefits:
      - Config-driven classes
      - Flexible component styling
    problems_solved:
      - Difficult style customization
      - Hard-coded component classes
    why: Allows per-element Tailwind class overrides from config
  related_decisions:
    depends_on: []
    see_also:
      - IMPL-CONFIG_LOADER
    supersedes: []
  status: Active
  traceability:
    architecture:
      - ARCH-CLASS_OVERRIDES
    code_annotations:
      - REQ-CONFIG_DRIVEN_UI
    requirements:
      - REQ-CONFIG_DRIVEN_UI
    tests:
      - src/lib/config.test.ts
