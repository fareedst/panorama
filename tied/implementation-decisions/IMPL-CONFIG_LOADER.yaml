IMPL-CONFIG_LOADER:
  code_locations:
    files:
      - description: Config loader implementation
        path: src/lib/config.ts
      - description: Config TypeScript types
        path: src/lib/config.types.ts
    functions:
      - description: Load and merge site config
        file: src/lib/config.ts
        name: getSiteConfig
      - description: Load and merge theme config
        file: src/lib/config.ts
        name: getThemeConfig
  cross_references:
    - ARCH-CONFIG_DRIVEN_UI
    - REQ-CONFIG_DRIVEN_UI
  decision: A server-only TypeScript module (`src/lib/config.ts`) reads YAML configuration files, deep-merges them with built-in defaults, caches the result, and exports typed accessor functions.
  implementation_approach:
    details:
      - Cache parsed configs
      - Deep merge user config with defaults
      - Define TypeScript interfaces
      - Handle missing files gracefully
      - Implement getSiteConfig() and getThemeConfig()
    summary: Create config loader module with deep merge and caching
  metadata:
    created:
      author: AI Agent
      date: {}
    last_updated:
      author: AI Agent
      date: '2026-05-29T00:00:00.000Z'
      reason: IMPL essence_pseudocode retrofit 2026-05-29
    last_validated:
      date: {}
      result: pass
      validator: AI Agent
  name: Configuration Loader Module
  rationale:
    benefits:
      - Automatic deep merge with defaults
      - Caching for performance
      - Type-safe config loading
    problems_solved:
      - Manual YAML parsing
      - Missing defaults
      - No type safety
    why: Provides typed configuration loading with deep merge and caching
  related_decisions:
    depends_on: []
    see_also:
      - IMPL-YAML_CONFIG
    supersedes: []
  status: Active
  traceability:
    architecture:
      - ARCH-CONFIG_DRIVEN_UI
    code_annotations:
      - REQ-CONFIG_DRIVEN_UI
    requirements:
      - REQ-CONFIG_DRIVEN_UI
    tests:
      - src/lib/config.test.ts
      - src/test/integration/app.test.tsx
