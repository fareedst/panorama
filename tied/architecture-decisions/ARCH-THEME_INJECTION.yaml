# ARCH detail — ARCH-THEME_INJECTION
ARCH-THEME_INJECTION:
  alternatives_considered: []
  cross_references:
    - REQ-CONFIG_DRIVEN_UI
  decision: 'Theme color values are injected into the document via a `<style>` tag rendered by the root layout component, replacing the previously hard-coded CSS custom property values in `globals.css`. The style tag contains `:root` declarations for light mode and `@media (prefers-color-scheme: dark)` declarations for dark mode, all sourced from `config/theme.yaml`.'
  implementation_approach:
    details:
      - Generate CSS variable string
      - Inject via style attribute
      - Read theme config in layout
    summary: Inject CSS variables in root layout from theme config
  metadata:
    created:
      author: AI Agent
      date: 2025-11-08T00:00:00.000Z
    last_updated:
      author: AI Agent
      date: 2026-02-06T00:00:00.000Z
      reason: Migration to v1.5.0
    last_validated:
      date: 2026-02-06T00:00:00.000Z
      result: pass
      validator: AI Agent
  name: CSS Variable Injection from Theme Config
  rationale:
    benefits:
      - Config-driven colors
      - Runtime theming
    problems_solved:
      - Hard-coded CSS variables
      - Static theme colors
    why: Inject CSS variables from theme config to enable runtime theming
  related_decisions:
    depends_on:
      - ARCH-CONFIG_DRIVEN_UI
    informs:
      - IMPL-THEME_INJECTION
    see_also:
      - ARCH-CSS_VARIABLES
  status: Active
  traceability:
    code_annotations:
      - REQ-CONFIG_DRIVEN_UI
    implementation:
      - IMPL-THEME_INJECTION
    requirements:
      - REQ-CONFIG_DRIVEN_UI
    tests: []
