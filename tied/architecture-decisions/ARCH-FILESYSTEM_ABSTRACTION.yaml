ARCH-FILESYSTEM_ABSTRACTION:
  alternatives_considered:
    - cons:
        - Limited to user's local files
        - Not server filesystem
      name: Client-side File System Access API
      pros:
        - Native browser support
      rejected_reason: Requirement is server filesystem browsing
  cross_references:
    - REQ-DIRECTORY_NAVIGATION
    - REQ-FILE_OPERATIONS
  decision: Data layer module (files.data.ts) wraps Node.js fs/promises API
  implementation_approach:
    details:
      - 'copyFile: recursive mkdir on dest parent; fs.cp for directories, fs.copyFile for files; attribute preservation via IMPL-COPY_ATTRS'
      - Error handling for permissions, non-existent paths
      - Path validation to prevent directory traversal
      - Server-only module (not imported by client components)
      - Use fs/promises for async operations
    summary: Data layer module (files.data.ts) wraps Node.js fs/promises API
  metadata:
    created:
      author: AI Agent
      date: {}
    last_updated:
      author: AI Agent
      date: '2026-06-02T00:00:00.000Z'
      reason: 'LEAP: copyFile tests in traceability'
    last_validated:
      date: {}
      result: pass
      validator: AI Agent
  name: Server-Side Filesystem Abstraction
  rationale:
    benefits:
      - Consistent API
      - Secure server-side access
      - Testable with mocks
    problems_solved:
      - Client-side filesystem access impossible
      - Difficult testing
      - Security vulnerabilities from direct fs access
    why: Server-side filesystem access via data layer ensures security and follows Next.js best practices
  related_decisions:
    depends_on:
      - ARCH-NEXTJS_FRAMEWORK
    informs:
      - IMPL-FILES_DATA
    see_also: []
  status: Active
  traceability:
    code_annotations:
      - ARCH-FILESYSTEM_ABSTRACTION
    implementation:
      - IMPL-FILES_DATA
    requirements:
      - REQ-DIRECTORY_NAVIGATION
      - REQ-FILE_OPERATIONS
    tests:
      - src/lib/copy-file.data.test.ts
      - src/lib/files.data.test.ts
